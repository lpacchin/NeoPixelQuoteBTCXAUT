# Function Reference — DATA-MODEL

> Data: `2026-08-15` · SHA: `98a053e`  
> Proiezione derivata da `funcref.views` — rigenerata ad ogni assemblaggio.

> Schema SQLite letto dai DB reali in `data/` in **sola lettura** (`sqlite_master` + `PRAGMA table_info`). Manifest: sole chiavi di primo livello. Sorgenti assenti → nota, nessuna invenzione (CONTRACT #1).

## Tabelle SQLite

> NOTA: `data/` non accessibile — schema DB non generato.

## Manifest (chiavi di primo livello)

> NOTA: `repo/manifest.yaml` non accessibile — struttura non generata.

## Shape dei dict ricorrenti

> Rimandata a Plan F (prima generazione full): richiede evidenza sorgente citabile per ogni shape (`A-`/`K-`/`T-`/`F-`, competency). Nessun campo inventato qui (CONTRACT #1).
