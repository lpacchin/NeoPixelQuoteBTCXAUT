# Function Reference — FLOWS (flussi end-to-end)

> Data: `2026-08-15` · SHA: `98a053e`  
> Proiezione derivata da `funcref.views` — rigenerata ad ogni assemblaggio.

> Sequenza meccanica ricostruita via `calls_out` (BFS), **profondità capata a 3**: oltre il cap la sequenza è troncata e lo dichiara. La narrazione è LLM, ancorata alla sequenza (evidenza vincolante); mai side-effect fuori dai segnali.

> NOTA: nessun flusso configurato per questo progetto. Per documentare i flussi chiave, forniscili via `--flows` (o env `FUNCREF_FLOWS`); senza di essi la skill non inventa flussi (CONTRACT #1).
