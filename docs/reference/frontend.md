# Function Reference — frontend (best-effort)

> Data: `2026-08-15` · SHA: `98a053e`  
> Proiezione derivata da `funcref.views` — rigenerata ad ogni assemblaggio.

> Il frontend è JS vanilla senza la rete AST-Python: questa mappa è ottenuta per **grep** dei letterali `/api/...`. **Ogni voce è non-verificata meccanicamente (parser JS assente)** — nessuna garanzia di completezza o correttezza (spec).

## Endpoint referenziati dal frontend

> NOTA: `static/index.html` non accessibile — mappa non generata.
